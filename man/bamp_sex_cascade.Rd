% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coherent_sex_cascade.R
\name{bamp_sex_cascade}
\alias{bamp_sex_cascade}
\title{Per-stratum disease cascade on a stratum-coherent total}
\usage{
bamp_sex_cascade(
  taxonomy,
  cases,
  population,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  periods_per_agegroup,
  deviation = "ar1",
  mcmc_total = list(iterations = 4000, burn_in = 1000, thin = 2),
  mcmc_cascade = list(iterations = 4000, burn_in = 1000, thin = 2),
  factor = NULL,
  order = "prevalence",
  prior_scale = TRUE,
  seed = 1
)
}
\arguments{
\item{taxonomy}{disease taxonomy, as in \code{\link{bamp_cascade}}.}

\item{cases}{named list with one element per stratum (e.g. \code{list(men=, women=)}); each element
is a named list of leaf-disease count matrices for that stratum (names = taxonomy leaves).}

\item{population}{named list with one \code{[periods x agegroups]} population matrix per stratum.}

\item{age, period, cohort, periods_per_agegroup}{model settings.}

\item{deviation}{deviation prior for the stratum-coherent total (\code{\link{bamp_coherent}}).}

\item{mcmc_total, mcmc_cascade}{MCMC settings for the coherent total and each cascade fit.}

\item{factor, order, prior_scale, seed}{passed to the cascade fits (\code{\link{bamp_cascade}}).}
}
\value{
object of class \code{apc_sex_cascade}: the stratum-coherent total model, the per-stratum
  cascades, and metadata; used by \code{\link{predict_sex_cascade}}.
}
\description{
Fit the full sex (or other exhaustive stratum) by disease-taxonomy model coherently: a
\code{\link{bamp_coherent}} model for the stratum all-cause totals (so the strata do not diverge)
plus a \code{\link{bamp_cascade}} disease taxonomy within each stratum. \code{\link{predict_sex_cascade}}
layers them into a hazard tensor that is coherent on both the stratum margin and the disease
margin -- the object an NCD microsimulation (e.g. IMPACTncd) consumes.
}
\seealso{
\code{\link{predict_sex_cascade}}, \code{\link{bamp_coherent}}, \code{\link{bamp_cascade}}
}
