% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{convergence_report}
\alias{convergence_report}
\title{Convergence pass/fail report across many fits}
\usage{
convergence_report(fits, rhat_max = 1.01, ess_min = 400)
}
\arguments{
\item{fits}{a named list of fitted objects (coherent / multicause / cascade ...).}

\item{rhat_max, ess_min}{thresholds passed to \code{\link{bamp_diagnostics}}.}
}
\value{
a list of class \code{bamp_convergence_report} with \code{table} (one row per fit:
  \code{fit}, \code{n_params}, \code{max_rhat}, \code{min_ess}, \code{n_flagged}, \code{ok})
  and \code{all_ok} (logical).
}
\description{
Run \code{\link{bamp_diagnostics}} on each of a named list of fits and tabulate the
worst split-\eqn{\hat R}, smallest ESS and flag count per fit -- the operational tool
for a 30-disease x strata sweep, where you cannot inspect every chain. Sorts the
worst fits first and reports an overall pass/fail.
}
\seealso{
\code{\link{bamp_diagnostics}}, \code{\link{validation_report}}
}
