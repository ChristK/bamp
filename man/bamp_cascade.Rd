% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coherent_cascade.R
\name{bamp_cascade}
\alias{bamp_cascade}
\title{Coherent competing-disease projection over a disease taxonomy (cascade)}
\usage{
bamp_cascade(
  taxonomy,
  cases,
  population,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  periods_per_agegroup,
  mcmc = list(iterations = 4000, burn_in = 1000, thin = 2),
  order = "prevalence",
  hyper = NULL,
  prior_scale = TRUE,
  seed = 1
)
}
\arguments{
\item{taxonomy}{named list mapping each group to a character vector of its leaf-disease names
(e.g. \code{list(CVD = c("CHD","stroke"), cancer = c("lung","colorectal"))}). Need >= 2 groups.}

\item{cases}{named list of leaf-disease count matrices (\code{[periods x agegroups]}); the names
must cover exactly the leaves in \code{taxonomy}. Leaves partition all deaths.}

\item{population}{a single shared \code{[periods x agegroups]} population matrix.}

\item{age, period, cohort, periods_per_agegroup}{model settings, as in \code{\link{bamp_multicause}}.}

\item{mcmc, order, hyper, prior_scale, seed}{passed to each \code{\link{bamp_multicause}} fit.}
}
\value{
object of class \code{apc_cascade}: the fitted group model, the per-group leaf models, the
  taxonomy, and metadata; used by \code{\link{predict_cascade}}.
}
\description{
Fit many competing diseases organised in a 2-level taxonomy (groups of leaf diseases) as a
coherent cascade: a group-level \code{\link{bamp_multicause}} (groups partition all-cause) plus,
within each group, a leaf-level \code{\link{bamp_multicause}} (leaves partition the group). The
projected leaf rates/hazards are coherent at \emph{every} level by construction. Because each fit
is small (a handful of groups; a handful of leaves per group), this scales to dozens of diseases
on the dense reference engine, where a single flat multinomial over all leaves would not.
}
\seealso{
\code{\link{predict_cascade}}, \code{\link{bamp_multicause}}, \code{\link{bamp_coherent}}
}
