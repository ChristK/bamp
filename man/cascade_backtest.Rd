% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{cascade_backtest}
\alias{cascade_backtest}
\title{Out-of-sample backtest of a disease-taxonomy cascade forecast}
\usage{
cascade_backtest(
  taxonomy,
  cases,
  population,
  holdout = 2,
  n_origins = 1,
  window = NULL,
  models = c("cascade", "independent", "naive", "leecarter"),
  scale = c("rate", "lograte"),
  vs_p = 0.5,
  levels = c(0.5, 0.8, 0.9, 0.95),
  draws_baseline = 2000,
  periods_per_agegroup,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  mcmc = list(iterations = 3000, burn_in = 1000, thin = 2),
  mcmc_bamp = list(number_of_iterations = 3000, burn_in = 1000, step = 2, tuning = 300),
  seed = 1
)
}
\arguments{
\item{taxonomy}{named list of groups, each a vector of leaf-disease names (as in \code{\link{bamp_cascade}}).}

\item{cases}{named list of leaf-disease count matrices; names must cover the taxonomy leaves.}

\item{population}{a single \code{[periods x agegroups]} population matrix.}

\item{holdout, n_origins, window}{rolling-origin controls (see \code{\link{multicause_backtest}}).}

\item{models}{any of \code{"cascade"}, \code{"independent"}, \code{"naive"}, \code{"leecarter"}.}

\item{scale, vs_p, levels, draws_baseline}{scoring controls (see \code{\link{multicause_backtest}}).}

\item{periods_per_agegroup, age, period, cohort, mcmc, mcmc_bamp, seed}{model / MCMC settings.}
}
\value{
a \code{bamp_backtest} object (\code{overall}, \code{per_origin}, and \code{per_cause}
  = the per-leaf breakdown for the cascade model).
}
\description{
Rolling-origin, multivariate-scored backtest of the coherent disease cascade
(\code{\link{bamp_cascade}}) against independent-APC, naive and Lee-Carter baselines,
scoring the \emph{joint} leaf-by-age forecast plus a per-leaf calibration breakdown.
The cascade analogue of \code{\link{multicause_backtest}}.
}
\seealso{
\code{\link{multicause_backtest}}, \code{\link{validation_report}}
}
