% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{combine_chains}
\alias{combine_chains}
\title{Pool multiple chains into a single fit for prediction}
\usage{
combine_chains(mc)
}
\arguments{
\item{mc}{a \code{bamp_multichain} from \code{\link{run_chains}}.}
}
\value{
a single fit object (e.g. \code{apc_coherent} / \code{apc_multicause}) with pooled draws.
}
\description{
Concatenate the kept draws of every chain along the iteration axis, returning a single
fit object (same class as the individual chains) usable by the \code{predict_*} functions
with the combined draws. For \code{\link{bamp_multicause}} the all-cause \code{total}
sub-fit is taken from the first chain (the field-share draws are fully pooled).
}
\seealso{
\code{\link{run_chains}}, \code{\link{bamp_diagnostics}}
}
