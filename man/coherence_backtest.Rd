% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coherence_backtest.R
\name{coherence_backtest}
\alias{coherence_backtest}
\title{Out-of-sample multivariate backtest of coherent sex forecasts}
\usage{
coherence_backtest(
  cases,
  population,
  holdout = 2,
  periods_per_agegroup,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  models = c("coherent", "independent", "totalshare"),
  deviation = "iid",
  rho = 0,
  scale = c("rate", "lograte"),
  vs_p = 0.5,
  mcmc_coherent = list(iterations = 4000, burn_in = 1000, thin = 2),
  mcmc_bamp = list(number_of_iterations = 4000, burn_in = 1000, step = 2, tuning = 300),
  seed = 1
)
}
\arguments{
\item{cases, population}{named lists of two \code{[periods x agegroups]} matrices (the sexes).}

\item{holdout}{number of final periods to hold out and forecast.}

\item{periods_per_agegroup, age, period, cohort}{model settings (as in \code{\link{bamp}}).}

\item{models}{which models to compare: any of \code{"coherent"}, \code{"independent"},
\code{"totalshare"}.}

\item{deviation, rho}{deviation prior for the coherent model (see \code{\link{bamp_coherent}}).}

\item{scale}{score on the \code{"rate"} or \code{"lograte"} scale (log rates clipped at 1e-6).}

\item{vs_p}{order of the variogram score.}

\item{mcmc_coherent, mcmc_bamp}{MCMC settings for \code{\link{bamp_coherent}} and \code{\link{bamp}}.}

\item{seed}{RNG seed.}
}
\value{
a data.frame with one row per model: mean held-out \code{energy} and \code{variogram}
  scores (joint; lower = better), \code{energy_marginal} (sum of per-sex energy scores), and a
  non-divergence diagnostic \code{gap_growth} = across-draw variance of the projected sex gap at
  the last held-out period divided by the first (\eqn{\approx 1} for the coherent model, \eqn{>1}
  for diverging independent fits) with its components \code{gap_var_first}/\code{gap_var_last}.
}
\description{
Hold out the last \code{holdout} periods, refit each model on the rest, project, and score the
\emph{joint} sex-by-age forecast at the held-out periods with proper multivariate rules
(\code{\link{energy_score}}, \code{\link{variogram_score}}) against the observed rates. Compares
the coherent joint model against the no-coupling \code{independent} baseline (and optionally the
Phase 0 \code{totalshare} wrapper). A marginal energy score is also reported to show that marginal
scoring is blind to the coherence (dependence) gain.
}
\seealso{
\code{\link{energy_score}}, \code{\link{variogram_score}}, \code{\link{select_rho}}
}
