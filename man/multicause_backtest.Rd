% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{multicause_backtest}
\alias{multicause_backtest}
\title{Out-of-sample backtest of competing-cause forecasts}
\usage{
multicause_backtest(
  cases,
  population,
  holdout = 2,
  n_origins = 1,
  window = NULL,
  models = c("coupled", "independent", "naive", "leecarter"),
  order = "prevalence",
  scale = c("rate", "lograte"),
  vs_p = 0.5,
  levels = c(0.5, 0.8, 0.9, 0.95),
  draws_baseline = 2000,
  periods_per_agegroup,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  mcmc = list(iterations = 3000, burn_in = 1000, thin = 2),
  mcmc_bamp = list(number_of_iterations = 3000, burn_in = 1000, step = 2, tuning = 300),
  seed = 1
)
}
\arguments{
\item{cases}{named list of \code{Cn} cause-specific \code{[periods x agegroups]} count matrices.}

\item{population}{a single \code{[periods x agegroups]} population matrix (shared denominator).}

\item{holdout}{number of final periods to forecast at each origin.}

\item{n_origins}{number of rolling origins (each steps the training end back by one period).}

\item{window}{fixed training-window length for a sliding origin; \code{NULL} = expanding.}

\item{models}{any of \code{"coupled"}, \code{"independent"}, \code{"naive"}, \code{"leecarter"}.}

\item{order}{cause ordering passed to \code{\link{bamp_multicause}}.}

\item{scale}{score on the \code{"rate"} or \code{"lograte"} scale.}

\item{vs_p}{order of the variogram score.}

\item{levels}{nominal central-interval levels for coverage.}

\item{draws_baseline}{ensemble size for the naive / Lee-Carter baselines.}

\item{periods_per_agegroup, age, period, cohort}{model settings (as in \code{\link{bamp}}).}

\item{mcmc, mcmc_bamp}{MCMC settings for \code{\link{bamp_multicause}} and \code{\link{bamp}}.}

\item{seed}{RNG seed.}
}
\value{
a list of class \code{bamp_backtest} with \code{overall} (data.frame: one row per
  model, origin-averaged \code{energy}, \code{variogram}, \code{crps}, \code{logs},
  \code{cov90}, \code{pit_p}), \code{per_origin} (per-origin scores), and \code{per_cause}
  (per-disease \code{energy}/\code{crps}/\code{cov90} for the coupled model; the C7 report).
}
\description{
Rolling-origin, multivariate-scored backtest of the coupled competing-cause model
(\code{\link{bamp_multicause}}) against independent-APC, naive random-walk and
Lee-Carter baselines. For each origin the models are refit on a training span and
the held-out periods projected; the \emph{joint} cause-by-age forecast is scored with
\code{\link{energy_score}} / \code{\link{variogram_score}} plus marginal
\code{\link{crps_sample}} and interval \code{\link{calibration}}. A per-cause
breakdown is returned so you can see which diseases are well- or badly-calibrated.
}
\seealso{
\code{\link{coherence_backtest}}, \code{\link{calibration}}, \code{\link{validation_report}}
}
