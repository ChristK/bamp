% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{run_chains}
\alias{run_chains}
\title{Run several independent chains of a bamp sampler}
\usage{
run_chains(
  FUN,
  args = list(),
  chains = 4,
  seeds = NULL,
  parallel = TRUE,
  cores = NULL
)
}
\arguments{
\item{FUN}{a fitting function, e.g. \code{\link{bamp_coherent}}, \code{\link{bamp_multicause}}
or \code{\link{bamp_cascade}} (it must take a \code{seed} argument).}

\item{args}{named list of arguments passed to \code{FUN} (do not include \code{seed}).}

\item{chains}{number of chains.}

\item{seeds}{integer seeds, one per chain; default \code{seq_len(chains)}.}

\item{parallel}{if \code{TRUE}, fork the chains with \code{parallel::mclapply} (no effect on Windows).}

\item{cores}{number of worker processes; default \code{min(chains, detectCores - 1)}.}
}
\value{
a list of class \code{bamp_multichain} with \code{chains} (the list of fits),
  \code{n_chains} and \code{seeds}. Pass it to \code{\link{bamp_diagnostics}} for
  between-chain \eqn{\hat R}, or \code{\link{combine_chains}} to pool draws for prediction.
}
\description{
Fit a coherent / competing-cause / cascade sampler several times from dispersed
seeds (optionally forked in parallel) so convergence can be judged with the true
between-chain \eqn{\hat R} via \code{\link{bamp_diagnostics}}.
}
\seealso{
\code{\link{bamp_diagnostics}}, \code{\link{combine_chains}}
}
