% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coherence_backtest.R
\name{select_rho}
\alias{select_rho}
\title{Select the AR1 coherence strength rho by out-of-sample score}
\usage{
select_rho(
  cases,
  population,
  holdout = 2,
  periods_per_agegroup,
  age = "rw1",
  period = "rw1",
  cohort = "rw1",
  rho_grid = c(0, 0.3, 0.6, 0.9),
  score = c("energy", "variogram"),
  scale = c("rate", "lograte"),
  vs_p = 0.5,
  mcmc_coherent = list(iterations = 4000, burn_in = 1000, thin = 2),
  seed = 1
)
}
\arguments{
\item{cases, population, holdout, periods_per_agegroup, age, period, cohort}{as in
\code{\link{coherence_backtest}}.}

\item{rho_grid}{grid of AR1 coefficients to evaluate (each in \code{[0, 1)}).}

\item{score}{which score to minimise: \code{"energy"} or \code{"variogram"}.}

\item{scale, vs_p, mcmc_coherent, seed}{as in \code{\link{coherence_backtest}}.}
}
\value{
list with \code{table} (data.frame of \code{rho} and its held-out scores) and
  \code{best_rho} (the minimiser of the chosen score).
}
\description{
Choose the deviation AR1 coefficient \code{rho} (the coherence dial of \code{\link{bamp_coherent}})
from the data, instead of fixing it, by minimising a held-out multivariate score over a grid. This
addresses the Jallbjorn caution: rather than asserting how strongly the sex gap reverts, let the
out-of-sample fit decide. \code{rho = 0} corresponds to the \code{"iid"} deviation.
}
\seealso{
\code{\link{coherence_backtest}}, \code{\link{bamp_coherent}}
}
