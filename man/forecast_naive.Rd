% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{forecast_naive}
\alias{forecast_naive}
\title{Naive random-walk-with-drift forecast ensemble (per-age baseline)}
\usage{
forecast_naive(
  cases,
  population,
  h,
  draws = 2000,
  scale = c("log", "logit"),
  eps = 1e-06
)
}
\arguments{
\item{cases, population}{training \code{[periods x agegroups]} count / population matrices.}

\item{h}{forecast horizon (number of future periods).}

\item{draws}{ensemble size.}

\item{scale}{\code{"log"} (log rate) or \code{"logit"} (log odds).}

\item{eps}{small rate floor before the log/logit transform.}
}
\value{
a list of length \code{h}; element \code{s} is a \code{draws x agegroups}
  rate ensemble for the \code{s}-th future period.
}
\description{
Independent random-walk-with-drift on each age's log (or logit) central rate:
drift and innovation sd estimated from the training first-differences, forecast
\eqn{s} steps ahead as \eqn{x_J + s\,\hat\delta + \sqrt{s}\,\hat\sigma Z}. The
simplest honest baseline a real model must beat.
}
\seealso{
\code{\link{forecast_leecarter}}, \code{\link{multicause_backtest}}
}
