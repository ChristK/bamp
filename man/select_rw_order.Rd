% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{select_rw_order}
\alias{select_rw_order}
\title{Select random-walk orders by out-of-sample score}
\usage{
select_rw_order(
  cases,
  population,
  periods_per_agegroup,
  holdout = 3,
  n_origins = 1,
  window = NULL,
  age = "rw1",
  period_orders = c("rw1", "rw2"),
  cohort_orders = c("rw1", "rw2"),
  score = c("energy", "crps"),
  scale = c("lograte", "rate"),
  mcmc_bamp = list(number_of_iterations = 3000, burn_in = 1000, step = 2, tuning = 300),
  seed = 1
)
}
\arguments{
\item{cases, population}{single \code{[periods x agegroups]} count / population matrices.}

\item{periods_per_agegroup}{period/age grid ratio (as in \code{\link{bamp}}).}

\item{holdout, n_origins, window}{rolling-origin controls (see \code{\link{multicause_backtest}}).}

\item{age}{fixed age order (usually \code{"rw1"} or \code{"rw2"}).}

\item{period_orders, cohort_orders}{candidate orders to compare.}

\item{score}{\code{"energy"} (joint over ages) or \code{"crps"} (marginal).}

\item{scale}{score on the \code{"lograte"} (default) or \code{"rate"} scale.}

\item{mcmc_bamp, seed}{MCMC settings / seed for \code{\link{bamp}}.}
}
\value{
a list with \code{table} (one row per period x cohort combination, mean held-out
  \code{energy} and \code{crps}) and \code{best} (the minimising \code{period}/\code{cohort}).
}
\description{
Choose the period and cohort prior order (\code{"rw1"} vs \code{"rw2"}) for a single-population
APC model from the data rather than by assumption, by rolling-origin out-of-sample scoring. RW1
forecasts a flat trend (constant + noise); RW2 continues the last linear trend (and over-extrapolates
at long horizons unless damped, see \code{\link{predict_apc}}'s \code{damping}). This refits each
candidate on a training span and scores the held-out age profile, returning the score table and the
minimiser -- the principled answer to "RW1 or RW2?".
}
\seealso{
\code{\link{multicause_backtest}}, \code{\link{predict_apc}}
}
