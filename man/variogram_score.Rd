% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{variogram_score}
\alias{variogram_score}
\title{Variogram score of a multivariate ensemble forecast}
\usage{
variogram_score(obs, ens, p = 0.5, weights = NULL)
}
\arguments{
\item{obs}{numeric observation vector (length d).}

\item{ens}{an \code{m x d} ensemble matrix.}

\item{p}{order of the variogram (default 0.5, robust to heavy tails).}

\item{weights}{optional \code{d x d} matrix of non-negative pair weights;
\code{NULL} weights all pairs equally.}
}
\value{
the variogram score (scalar; lower is better).
}
\description{
Proper multivariate scoring rule of order \code{p} (Scheuerer & Hamill 2015):
\deqn{VS_p = \sum_{k<l} w_{kl}\,\bigl(|y_k-y_l|^p - \tfrac1m\sum_i |X_{i,k}-X_{i,l}|^p\bigr)^2.}
It compares observed and forecast pairwise differences, so it targets the
dependence (here, cross-stratum / cross-age) structure directly. Lower is better.
}
\seealso{
\code{\link{energy_score}}, \code{\link{coherence_backtest}}
}
